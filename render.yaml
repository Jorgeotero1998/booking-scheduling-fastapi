services:
  - type: web
    name: booking-scheduling-fastapi-api
    env: docker
    plan: free
    autoDeploy: true
    dockerfilePath: docker/api/Dockerfile
    preDeployCommand: alembic upgrade head
    healthCheckPath: /api/v1/health
    envVars:
      - key: ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: booking-scheduling-fastapi-db
          property: connectionString
      - key: LLM_ENABLED
        value: "false"

databases:
  - name: booking-scheduling-fastapi-db
    plan: free
    databaseName: app
    user: postgres
